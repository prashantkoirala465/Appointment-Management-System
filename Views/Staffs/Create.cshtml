@model AppointmentSystem.Web.Models.Staff
@{
    ViewData["Title"] = "Add Staff Member";
}

<div style="margin-bottom: var(--space-xl);">
    <h1 class="page-title">Add Staff Member</h1>
    <p class="page-subtitle">Enter the new team member’s information below.</p>
</div>

<div class="card-custom" style="max-width:540px;">
    <div class="card-body-custom">
        <form asp-action="Create" class="form-custom">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <div class="form-group">
                <label asp-for="FullName">Full Name</label>
                <input asp-for="FullName" class="form-input" placeholder="Dr. Jane Smith" />
                <span asp-validation-for="FullName" class="text-danger"></span>
            </div>

            <div style="display:grid; grid-template-columns:1fr 1fr; gap:var(--space-md);">
                <div class="form-group">
                    <label asp-for="Email">Email</label>
                    <input asp-for="Email" class="form-input" placeholder="email@example.com" />
                    <span asp-validation-for="Email" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="PhoneNumber">Phone</label>
                    <input asp-for="PhoneNumber" class="form-input" placeholder="+1 (555) 000-0000" />
                    <span asp-validation-for="PhoneNumber" class="text-danger"></span>
                </div>
            </div>

            <div class="form-group">
                <label asp-for="Specialty">Specialty</label>
                <input asp-for="Specialty" class="form-input" placeholder="e.g. General Medicine" />
                <span asp-validation-for="Specialty" class="text-danger"></span>
            </div>

            <div class="form-group">
                <div class="form-check-custom">
                    <input asp-for="IsActive" />
                    <label asp-for="IsActive">Active</label>
                </div>
            </div>

            <div class="btn-group-custom" style="margin-top:var(--space-lg);">
                <button type="submit" class="btn-primary-custom">Add Staff</button>
                <a class="btn-secondary-custom" asp-action="Index">Cancel</a>
            </div>
        </form>
    </div>
</div>
