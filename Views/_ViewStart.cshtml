@{
    // This file is executed before every view in our application
    // It sets the default layout page that wraps all our views
    // This way we don't have to specify the layout in every single view file
    // The _Layout.cshtml contains the header, navigation, footer, etc.
    Layout = "_Layout";
}
